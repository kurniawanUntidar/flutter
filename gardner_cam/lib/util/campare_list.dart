import 'package:flutter/material.dart';

class compareList extends StatelessWidget {
  const compareList({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text(
        'Difference with std No',
        style: TextStyle(color: Colors.black),
      ),
    );
  }
}
